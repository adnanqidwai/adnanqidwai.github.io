---
import Layout from "../layouts/Layout.astro";
import { getCollection } from "astro:content";

const thoughtPosts = (
	await getCollection("writings", ({ data }) => !data.draft)
)
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
	.slice(0, 5);

const thoughtDateFmt = new Intl.DateTimeFormat("en-US", {
	year: "numeric",
	month: "short",
	day: "2-digit",
});

const baseUrl = import.meta.env.BASE_URL;
const localBaseUrl = baseUrl.replace(/\/$/, "");
const thoughtsBase = `${baseUrl.replace(/\/$/, "")}/thoughts/`;
const projectsBase = `${localBaseUrl}/projects/`;
const emailAddress = "adgta25@gmail.com";
const siteUrl = "https://adnanqidwai.github.io";
const profileImageUrl = `${siteUrl}/about_me/me.jpeg`;
const profileDescription = "Adnan Qidwai is a Research Engineer at IBM Research working on retrieval optimization, RAG systems, agentic data systems, semantic data pipelines, multimodal reasoning, and long-horizon agent evaluation.";

const publications = [
	{
		venue: "ACM CAIS 2026",
		title: "CAMI: Practical Cost-Aware Agent-Guided Multi-Indexing for Semantic Retrieval",
		note: "Efficient offline exploration of multi-index retrieval methods.",
		url: "https://www.caisconf.org/program/2026/papers/cami-cost-aware-agent-guided-multi-indexing-for-semantic-retrieval/",
	},
	{
		venue: "EMNLP 2024",
		title: "Unraveling the Truth: Do VLMs really Understand Charts?",
		note: "Consistency and robustness in chart understanding.",
		url: "https://aclanthology.org/2024.findings-emnlp.973/",
	},
	{
		venue: "ACL 2025",
		title: "PRAISE: Enhancing Product Descriptions with LLM-Driven Structured Insights",
		note: "Structured insights from reviews and seller descriptions.",
		url: "https://aclanthology.org/2025.acl-demo.62/",
	},
	{
		venue: "IJCNLP-AACL 2025",
		title: "InterChart: Benchmarking Visual Reasoning Across Decomposed and Distributed Chart Information",
		note: "Reasoning across related charts.",
		url: "https://aclanthology.org/2025.ijcnlp-long.111/",
	},
	{
		venue: "Middleware 2025",
		title: "LLM-Aided Customizable Profiling of Code Data Based On Programming Language Concepts",
		note: "LLMs plus static rules for code-data profiling.",
		url: "https://arxiv.org/abs/2503.15571",
	},
];

const links = [
	{ label: "Google Scholar", href: "https://scholar.google.com/citations?user=YZhLACgAAAAJ&hl=en", icon: "GS" },
	{ label: "GitHub", href: "https://github.com/adnanqidwai", icon: "GH" },
	{ label: "LinkedIn", href: "https://linkedin.com/in/adnanqidwai", icon: "LI" },
	{ label: "X", href: "https://x.com/adnanqidw_ai", icon: "X" },
	{ label: "Copy Email", copyValue: emailAddress, icon: "@" },
];

const personSameAs = links
	.filter((link): link is { label: string; href: string; icon: string; external?: boolean } => "href" in link && link.href.startsWith("http"))
	.map((link) => link.href);

const profileJsonLd = {
	"@context": "https://schema.org",
	"@type": "ProfilePage",
	"@id": `${siteUrl}/#profile`,
	url: siteUrl,
	name: "Adnan Qidwai",
	description: profileDescription,
	image: profileImageUrl,
	mainEntity: {
		"@type": "Person",
		"@id": `${siteUrl}/#person`,
		name: "Adnan Qidwai",
		url: siteUrl,
		image: profileImageUrl,
		email: `mailto:${emailAddress}`,
		jobTitle: "Research Engineer",
		worksFor: {
			"@type": "Organization",
			name: "IBM Research",
			url: "https://research.ibm.com/",
		},
		alumniOf: {
			"@type": "CollegeOrUniversity",
			name: "International Institute of Information Technology Hyderabad",
			url: "https://www.iiit.ac.in/",
		},
		knowsAbout: [
			"retrieval optimization",
			"RAG systems",
			"semantic data pipelines",
			"agentic data systems",
			"multimodal reasoning",
			"long-horizon agent evaluation",
			"code data profiling",
		],
		sameAs: personSameAs,
	},
};
---

<Layout
	title="Adnan Qidwai"
	description={profileDescription}
	type="profile"
	structuredData={profileJsonLd}
	showNav={false}
>
	<div class="win98-desktop" data-desktop>
		<div class="desktop-layout">
			<div class="stack stack-main">
				<section
					class="window about-window is-active"
					id="about-window"
					data-window
					aria-label="About Me window"
				>
					<header class="title-bar" data-focus-handle data-drag-handle>
						<span class="title-text">About Me</span>
					</header>
					<div class="window-body profile-body">
						<div class="profile-photo-frame">
							<img
								src="/about_me/me.jpeg"
								alt="Adnan Qidwai"
								class="profile-photo"
								width="732"
								height="732"
								decoding="async"
								fetchpriority="high"
							/>
						</div>
						<div class="profile-text">
							<h1 class="sr-only">Adnan Qidwai</h1>
							<p class="caption" aria-hidden="true">u/adnanqidwai/about.md</p>
							<p>
								Hi! I'm Adnan. I currently work at IBM Research as a
								Research Engineer in the Data for AI team. My work is
								centered on retrieval optimization and streamlining how
								models ingest and utilize large amounts of knowledge across
								many structured and unstructured sources.
							</p>
							<p>
								Beyond my core role, I'm interested in building
								long-horizon agents, with a particular focus on curating
								the right training data and creating meaningful metrics to
								measure their progress. I also enjoy probing frontier models
								to understand their limits and identifying the specific gaps
								where their reasoning or reliability starts to drop off.
							</p>
							<p>
								In the past, I worked on multimodal understanding in
								collaboration with
								<a href="https://vgupta123.github.io/" target="_blank" rel="noopener noreferrer">Prof. Vivek Gupta</a>
								(ASU),
								<a href="https://www.cis.upenn.edu/~danroth/" target="_blank" rel="noopener noreferrer">Prof. Dan Roth</a>
								(UPenn), and
								<a href="https://research.adobe.com/" target="_blank" rel="noopener noreferrer">Adobe Research</a>.
								I also spent time researching LLM jailbreaking as part of
								the Stanford AI Researcher project.
							</p>
							<p class="reach-out">
								I'm always open to discussing research, new opportunities,
								or just exchanging ideas. Feel free to reach out!
							</p>
							<div class="profile-actions" aria-label="Primary profile links">
								<a class="win-button" href="https://drive.google.com/file/d/1HJp7LFqF8L9sdKEN3YAokNzd0Crc3AF2/view?usp=sharing" target="_blank" rel="noopener noreferrer">CV</a>
								<a class="win-button" href={projectsBase}>projects</a>
							</div>
						</div>
					</div>
				</section>

				<section
					class="window work-window"
					id="work-window"
					data-window
					aria-label="What I like to work on window"
				>
					<header class="title-bar" data-focus-handle data-drag-handle>
						<span class="title-text">What I like to work on</span>
					</header>
					<div class="window-body">
						<p class="caption">Click a thread:</p>
						<div class="thread-buttons" role="tablist" aria-label="Research threads">
							<button type="button" class="win-button is-selected" data-thread="planning" role="tab" aria-selected="true">planning in agents</button>
							<button type="button" class="win-button" data-thread="retrieval" role="tab" aria-selected="false">retrieval systems</button>
							<button type="button" class="win-button" data-thread="enterprise" role="tab" aria-selected="false">context for agents</button>
							<button type="button" class="win-button" data-thread="semantic" role="tab" aria-selected="false">semantic data pipelines</button>
							<button type="button" class="win-button" data-thread="multimodal" role="tab" aria-selected="false">multimodal robustness</button>
						</div>
						<div class="dos-panel" data-thread-output role="tabpanel" aria-live="polite"></div>
					</div>
				</section>
			</div>

			<div class="stack stack-side">
				<section
					class="window papers-window"
					id="papers-window"
					data-window
					aria-label="Publications window"
				>
					<header class="title-bar" data-focus-handle data-drag-handle>
						<span class="title-text">Publications</span>
					</header>
					<div class="window-body">
						<div class="toolbar">
							<span class="toolbar-hint" aria-hidden="true">{publications.length} items</span>
						</div>
						<div class="publication-scroll" data-scroll-shell>
							<div class="publication-list" data-scroll-content tabindex="0" aria-label="Publications list">
								{publications.map((publication) => (
									<article class="publication">
										<div class="venue">{publication.venue}</div>
										<h2>
											<a
												href={publication.url}
												target="_blank"
												rel="noopener noreferrer"
											>
												{publication.title}
											</a>
										</h2>
										<p class="note">{publication.note}</p>
									</article>
								))}
							</div>
							<div class="publication-scrollbar" data-scroll-track aria-hidden="true">
								<span class="publication-scrollbar-thumb" data-scroll-thumb></span>
							</div>
						</div>
					</div>
				</section>

				<section
					class="window links-window"
					id="links-window"
					data-window
					aria-label="Links window"
				>
					<header class="title-bar" data-focus-handle data-drag-handle>
						<span class="title-text">Links</span>
					</header>
					<div class="window-body">
						<div class="link-list">
							{links.map((link) => (
								"copyValue" in link ? (
									<button
										type="button"
										data-copy-email
										data-copy-value={link.copyValue}
										aria-describedby="copy-toast"
									>
										<span class="small-icon" aria-hidden="true">{link.icon}</span>
										<span>{link.label}</span>
									</button>
								) : (
									<a
										href={link.href}
										target={link.external === false ? undefined : "_blank"}
										rel={link.external === false ? undefined : "noopener noreferrer"}
									>
										<span class="small-icon" aria-hidden="true">{link.icon}</span>
										<span>{link.label}</span>
									</a>
								)
							))}
						</div>
					</div>
				</section>

				<section
					class="window thoughts-window"
					id="thoughts-window"
					data-window
					aria-label="Thoughts window"
				>
					<header class="title-bar" data-focus-handle data-drag-handle>
						<span class="title-text">Thoughts</span>
					</header>
					<div class="window-body">
						<div class="toolbar">
							<a class="win-button" href={thoughtsBase}>Open all</a>
							<span class="toolbar-hint" aria-hidden="true">{thoughtPosts.length} {thoughtPosts.length === 1 ? "item" : "items"}</span>
						</div>
						{thoughtPosts.length === 0 ? (
							<p class="caption">No posts yet.</p>
						) : (
							<ul class="thoughts-list">
								{thoughtPosts.map((post) => (
									<li class="thoughts-item">
										<a class="thoughts-link" href={`${thoughtsBase}${post.slug}/`}>
											<span class="thoughts-date">
												{thoughtDateFmt.format(post.data.pubDate)}
											</span>
											<span class="thoughts-title">{post.data.title}</span>
										</a>
									</li>
								))}
							</ul>
						)}
					</div>
				</section>
			</div>
		</div>

		<div
			class="copy-toast"
			id="copy-toast"
			data-copy-toast
			role="status"
			aria-live="polite"
			aria-atomic="true"
			hidden
		>
			<div class="copy-toast-text">
				<strong data-copy-toast-title>Email copied</strong>
				<input
					class="copy-toast-input"
					type="text"
					value={emailAddress}
					readonly
					data-copy-toast-input
					aria-label="Email address"
				/>
			</div>
			<button type="button" class="copy-toast-close" data-copy-toast-close aria-label="Dismiss message">Close</button>
		</div>
	</div>
</Layout>

<script>
	const threadCopy: Record<string, { head: string; body: string }> = {
		planning: {
			head: "planning in agents",
			body: "Studying how long-horizon agents break through puzzles that test planning, memory, and unsolvability detection. Do they report their failures, or do they continue following instructions? Paper under review.",
		},
		retrieval: {
			head: "retrieval systems",
			body: "Working on multi-index retrieval methods and vectorless retrieval.",
		},
		enterprise: {
			head: "context for agents",
			body: "Building a rich layer for agents to talk to data spanning many structured and unstructured sources so that agents can reason with the right context.",
		},
		semantic: {
			head: "semantic data pipelines",
			body: "Optimizing LLM-powered semantic data processing pipelines for Pareto optimality over cost and accuracy",
		},
		multimodal: {
			head: "multimodal robustness",
			body: "Mostly focused on VLM understanding and robustness: whether models actually understand charts and visual information, where they break, and how to evaluate reasoning under the surface ",
		},
	};

	const prefersReducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
	const dragQuery = window.matchMedia("(hover: hover) and (pointer: fine)");
	const clipboardButtons = Array.from(document.querySelectorAll<HTMLButtonElement>("[data-copy-email]"));
	const copyToast = document.querySelector<HTMLElement>("[data-copy-toast]");
	const copyToastTitle = document.querySelector<HTMLElement>("[data-copy-toast-title]");
	const copyToastInput = document.querySelector<HTMLInputElement>("[data-copy-toast-input]");
	const copyToastClose = document.querySelector<HTMLButtonElement>("[data-copy-toast-close]");
	let copyToastTimer: number | null = null;

	const windows = Array.from(document.querySelectorAll<HTMLElement>("[data-window]"));
	let topZ = 10;

	function copyWithSelectionFallback(text: string) {
		const textarea = document.createElement("textarea");
		textarea.value = text;
		textarea.setAttribute("readonly", "");
		textarea.style.position = "fixed";
		textarea.style.inset = "0 auto auto 0";
		textarea.style.width = "1px";
		textarea.style.height = "1px";
		textarea.style.opacity = "0";
		document.body.appendChild(textarea);
		textarea.focus({ preventScroll: true });
		textarea.select();
		textarea.setSelectionRange(0, text.length);
		const copied = document.execCommand("copy");
		textarea.remove();
		return copied;
	}

	async function writeClipboardText(text: string) {
		try {
			if (copyWithSelectionFallback(text)) return true;
		} catch {
			// Continue to the Clipboard API fallback.
		}

		if (!navigator.clipboard?.writeText) return false;

		const modernCopy = navigator.clipboard
			.writeText(text)
			.then(() => true)
			.catch(() => false);
		const timeout = new Promise<boolean>((resolve) => {
			window.setTimeout(() => resolve(false), 700);
		});
		return Promise.race([modernCopy, timeout]);
	}

	function showCopyToast(title: string, detail: string, isError = false, selectValue = false) {
		if (!copyToast || !copyToastTitle || !copyToastInput) return;
		copyToastTitle.textContent = title;
		copyToastInput.value = detail;
		copyToast.classList.toggle("is-error", isError);
		copyToast.hidden = false;
		requestAnimationFrame(() => {
			copyToast.classList.add("is-visible");
			if (selectValue) {
				copyToastInput.focus({ preventScroll: true });
				copyToastInput.select();
			}
		});
		if (copyToastTimer !== null) window.clearTimeout(copyToastTimer);
		copyToastTimer = selectValue ? null : window.setTimeout(() => hideCopyToast(), 3200);
	}

	function hideCopyToast() {
		if (!copyToast) return;
		copyToast.classList.remove("is-visible");
		if (copyToastTimer !== null) {
			window.clearTimeout(copyToastTimer);
			copyToastTimer = null;
		}
		window.setTimeout(() => {
			if (!copyToast.classList.contains("is-visible")) copyToast.hidden = true;
		}, prefersReducedMotion ? 0 : 180);
	}

	clipboardButtons.forEach((button) => {
		button.addEventListener("click", async () => {
			const value = button.dataset.copyValue;
			if (!value) return;
			const copied = await writeClipboardText(value);
			if (copied) {
				showCopyToast("Email copied", value);
				return;
			}
			showCopyToast("Copy manually", value, false, true);
		});
	});

	copyToastClose?.addEventListener("click", hideCopyToast);

	function focusWindow(win: HTMLElement) {
		windows.forEach((item) => item.classList.remove("is-active"));
		win.classList.add("is-active");
		topZ += 1;
		win.style.zIndex = String(topZ);
	}

	function isDragEnabled() {
		return dragQuery.matches && window.innerWidth > 760;
	}

	function clamp(value: number, min: number, max: number) {
		return Math.min(Math.max(value, min), max);
	}

	function resetWindowDrag(win: HTMLElement) {
		win.classList.remove("is-dragging");
		delete win.dataset.dragX;
		delete win.dataset.dragY;
		win.style.transform = "";
	}

	const dragHandles = Array.from(document.querySelectorAll<HTMLElement>("[data-drag-handle]"));
	let activeDragWindow: HTMLElement | null = null;

	function canStartWindowDrag(target: EventTarget | null) {
		if (!isDragEnabled()) return false;
		if (!(target instanceof HTMLElement)) return true;
		return !target.closest("a, button, input, textarea, select");
	}

	function isWindowDragButton(button: number) {
		return button === 0 || button === 1;
	}

	function beginWindowDrag(win: HTMLElement, startX: number, startY: number) {
		if (activeDragWindow) return null;
		activeDragWindow = win;
		focusWindow(win);
		win.classList.add("is-dragging");

		const originX = Number(win.dataset.dragX ?? 0);
		const originY = Number(win.dataset.dragY ?? 0);
		const startRect = win.getBoundingClientRect();
		const visibleMargin = 72;
		const minX = originX + visibleMargin - startRect.right;
		const maxX = originX + window.innerWidth - visibleMargin - startRect.left;
		const minY = originY + 8 - startRect.top;
		const maxY = originY + window.innerHeight - visibleMargin - startRect.top;

		return {
			move(clientX: number, clientY: number) {
				const nextX = clamp(originX + clientX - startX, minX, maxX);
				const nextY = clamp(originY + clientY - startY, minY, maxY);
				win.dataset.dragX = String(nextX);
				win.dataset.dragY = String(nextY);
				win.style.transform = `translate3d(${nextX}px, ${nextY}px, 0)`;
			},
			end() {
				win.classList.remove("is-dragging");
				activeDragWindow = null;
			},
		};
	}

	dragHandles.forEach((handle) => {
		const win = handle.closest<HTMLElement>("[data-window]");
		if (!win) return;

		handle.addEventListener("pointerdown", (event) => {
			if (!isWindowDragButton(event.button) || !canStartWindowDrag(event.target)) return;
			event.preventDefault();
			const drag = beginWindowDrag(win, event.clientX, event.clientY);
			if (!drag) return;
			handle.setPointerCapture(event.pointerId);

			const onPointerMove = (moveEvent: PointerEvent) => {
				drag.move(moveEvent.clientX, moveEvent.clientY);
			};

			const onPointerUp = () => {
				drag.end();
				if (handle.hasPointerCapture(event.pointerId)) {
					handle.releasePointerCapture(event.pointerId);
				}
				handle.removeEventListener("pointermove", onPointerMove);
				handle.removeEventListener("pointerup", onPointerUp);
				handle.removeEventListener("pointercancel", onPointerUp);
			};

			handle.addEventListener("pointermove", onPointerMove);
			handle.addEventListener("pointerup", onPointerUp);
			handle.addEventListener("pointercancel", onPointerUp);
		});

		handle.addEventListener("mousedown", (event) => {
			if (!isWindowDragButton(event.button) || !canStartWindowDrag(event.target)) return;
			event.preventDefault();
			const drag = beginWindowDrag(win, event.clientX, event.clientY);
			if (!drag) return;

			const onMouseMove = (moveEvent: MouseEvent) => {
				drag.move(moveEvent.clientX, moveEvent.clientY);
			};

			const onMouseUp = () => {
				drag.end();
				window.removeEventListener("mousemove", onMouseMove);
				window.removeEventListener("mouseup", onMouseUp);
			};

			window.addEventListener("mousemove", onMouseMove);
			window.addEventListener("mouseup", onMouseUp);
		});

		handle.addEventListener("dblclick", () => resetWindowDrag(win));
	});

	const threadOutput = document.querySelector<HTMLElement>("[data-thread-output]");
	const threadButtons = Array.from(document.querySelectorAll<HTMLButtonElement>("[data-thread]"));
	let typeTimer: number | null = null;

	function renderThread(key: string) {
		if (!threadOutput || !threadCopy[key]) return;
		threadButtons.forEach((btn) => {
			const selected = btn.dataset.thread === key;
			btn.classList.toggle("is-selected", selected);
			btn.setAttribute("aria-selected", selected ? "true" : "false");
		});
		const { head, body } = threadCopy[key];
		if (typeTimer !== null) {
			window.clearInterval(typeTimer);
			typeTimer = null;
		}
		if (prefersReducedMotion) {
			threadOutput.innerHTML = `<span class="prompt">&gt;&nbsp;${head}</span><br />${body}<span class="caret" aria-hidden="true"></span>`;
			return;
		}
		threadOutput.innerHTML = `<span class="prompt">&gt;&nbsp;${head}</span><br /><span class="typed"></span><span class="caret" aria-hidden="true"></span>`;
		const typedEl = threadOutput.querySelector<HTMLElement>(".typed");
		if (!typedEl) return;
		let i = 0;
		typeTimer = window.setInterval(() => {
			if (i >= body.length) {
				if (typeTimer !== null) {
					window.clearInterval(typeTimer);
					typeTimer = null;
				}
				return;
			}
			typedEl.textContent = body.slice(0, i + 1);
			i += 1;
		}, 14);
	}

	threadButtons.forEach((button) => {
		button.addEventListener("click", () => {
			const key = button.dataset.thread;
			if (key) renderThread(key);
		});
	});

	renderThread("planning");

	const scrollShells = Array.from(document.querySelectorAll<HTMLElement>("[data-scroll-shell]"));

	function updateScrollThumb(shell: HTMLElement) {
		const content = shell.querySelector<HTMLElement>("[data-scroll-content]");
		const track = shell.querySelector<HTMLElement>("[data-scroll-track]");
		if (!content || !track) return;
		const maxScroll = Math.max(0, content.scrollHeight - content.clientHeight);
		const trackHeight = Math.max(1, track.clientHeight);
		const thumbHeight = maxScroll === 0
			? trackHeight
			: Math.max(44, Math.round((content.clientHeight / content.scrollHeight) * trackHeight));
		const thumbTop = maxScroll === 0
			? 0
			: Math.round((content.scrollTop / maxScroll) * (trackHeight - thumbHeight));
		shell.style.setProperty("--scroll-thumb-height", `${thumbHeight}px`);
		shell.style.setProperty("--scroll-thumb-top", `${thumbTop}px`);
	}

	function scrollContentFromThumbTop(shell: HTMLElement, thumbTop: number) {
		const content = shell.querySelector<HTMLElement>("[data-scroll-content]");
		const track = shell.querySelector<HTMLElement>("[data-scroll-track]");
		const thumb = shell.querySelector<HTMLElement>("[data-scroll-thumb]");
		if (!content || !track || !thumb) return;
		const maxScroll = Math.max(0, content.scrollHeight - content.clientHeight);
		const maxThumbTop = Math.max(1, track.clientHeight - thumb.offsetHeight);
		const clampedThumbTop = Math.min(Math.max(thumbTop, 0), maxThumbTop);
		content.scrollTop = (clampedThumbTop / maxThumbTop) * maxScroll;
		updateScrollThumb(shell);
	}

	scrollShells.forEach((shell) => {
		const content = shell.querySelector<HTMLElement>("[data-scroll-content]");
		const track = shell.querySelector<HTMLElement>("[data-scroll-track]");
		const thumb = shell.querySelector<HTMLElement>("[data-scroll-thumb]");
		updateScrollThumb(shell);
		content?.addEventListener("scroll", () => updateScrollThumb(shell), { passive: true });
		track?.addEventListener("pointerdown", (event) => {
			if (!track || !thumb || event.target === thumb) return;
			event.preventDefault();
			const trackRect = track.getBoundingClientRect();
			scrollContentFromThumbTop(shell, event.clientY - trackRect.top - thumb.offsetHeight / 2);
		});
		thumb?.addEventListener("pointerdown", (event) => {
			if (!track || !thumb || !content) return;
			event.preventDefault();
			event.stopPropagation();
			thumb.setPointerCapture(event.pointerId);
			shell.classList.add("is-scroll-dragging");
			const startY = event.clientY;
			const startScrollTop = content.scrollTop;
			const maxScroll = Math.max(1, content.scrollHeight - content.clientHeight);
			const maxThumbTop = Math.max(1, track.clientHeight - thumb.offsetHeight);
			const onPointerMove = (moveEvent: PointerEvent) => {
				const deltaY = moveEvent.clientY - startY;
				content.scrollTop = startScrollTop + (deltaY / maxThumbTop) * maxScroll;
				updateScrollThumb(shell);
			};
			const onPointerUp = () => {
				shell.classList.remove("is-scroll-dragging");
				if (thumb.hasPointerCapture(event.pointerId)) {
					thumb.releasePointerCapture(event.pointerId);
				}
				thumb.removeEventListener("pointermove", onPointerMove);
				thumb.removeEventListener("pointerup", onPointerUp);
				thumb.removeEventListener("pointercancel", onPointerUp);
			};
			thumb.addEventListener("pointermove", onPointerMove);
			thumb.addEventListener("pointerup", onPointerUp);
			thumb.addEventListener("pointercancel", onPointerUp);
		});
	});

	window.addEventListener("resize", () => {
		scrollShells.forEach(updateScrollThumb);
		if (!isDragEnabled()) windows.forEach(resetWindowDrag);
	});

	dragQuery.addEventListener("change", () => {
		if (!isDragEnabled()) windows.forEach(resetWindowDrag);
	});

	windows.forEach((win) => {
		win.addEventListener("pointerdown", () => focusWindow(win));
	});
</script>

<style>
	:global(html) {
		background: #111111;
	}

	.win98-desktop,
	.win98-desktop * {
		box-sizing: border-box;
	}

	.sr-only {
		position: absolute;
		width: 1px;
		height: 1px;
		padding: 0;
		margin: -1px;
		overflow: hidden;
		clip: rect(0, 0, 0, 0);
		white-space: nowrap;
		border: 0;
	}

	.win98-desktop {
		--desktop-bg: #111111;
		--desktop-grid: rgba(255, 255, 255, 0.045);
		--window-bg: #e6e2d8;
		--window-panel: #f1eee7;
		--window-light: #ffffff;
		--window-mid: #9b968c;
		--window-shadow: #343230;
		--ink: #111111;
		--ink-muted: #4b4740;
		--ink-link: #1a4a8a;
		--title-start: #111111;
		--title-end: #5a574f;
		--title-inactive-start: #6c6860;
		--title-inactive-end: #9b968c;
		--terminal-text: #f4efe6;
		--terminal-accent: #8cc98c;
		position: relative;
		min-height: 100svh;
		overflow-x: clip;
		padding: 32px 28px 44px;
		background:
			radial-gradient(1200px 700px at 78% -10%, rgba(120, 108, 96, 0.18), transparent 60%),
			radial-gradient(900px 500px at -10% 110%, rgba(80, 70, 60, 0.22), transparent 65%),
			linear-gradient(90deg, var(--desktop-grid) 1px, transparent 1px),
			linear-gradient(var(--desktop-grid) 1px, transparent 1px),
			var(--desktop-bg);
		background-size: auto, auto, 34px 34px, 34px 34px, auto;
		color: var(--ink);
		font-family: "MS Sans Serif", Tahoma, Geneva, sans-serif;
		font-size: 13px;
	}

	.desktop-layout {
		position: relative;
		z-index: 2;
		display: grid;
		grid-template-columns: minmax(620px, 1.45fr) minmax(320px, 0.85fr);
		grid-template-areas: "main side";
		gap: 22px;
		max-width: 1260px;
		margin: 0 auto;
		align-items: start;
		min-width: 0;
	}

	.desktop-layout > * { min-width: 0; }

	.stack {
		display: flex;
		flex-direction: column;
		gap: 20px;
		min-width: 0;
	}

	.stack-main { grid-area: main; }
	.stack-side { grid-area: side; }

	/* Kept as structural hints; used when inner stacks collapse on narrow viewports. */
	.about-window { grid-area: about; }
	.work-window { grid-area: work; }
	.papers-window { grid-area: papers; }
	.links-window { grid-area: links; }

	.window {
		position: relative;
		z-index: 4;
		width: 100%;
		min-width: 0;
		max-width: 100%;
		box-sizing: border-box;
		background: var(--window-bg);
		border-top: 2px solid var(--window-light);
		border-left: 2px solid var(--window-light);
		border-right: 2px solid var(--window-shadow);
		border-bottom: 2px solid var(--window-shadow);
		box-shadow: 3px 3px 0 #000, 6px 6px 24px rgba(0, 0, 0, 0.35);
	}


	.window.is-active {
		z-index: 20;
	}

	.title-bar {
		display: flex;
		align-items: center;
		justify-content: flex-start;
		gap: 12px;
		min-height: 26px;
		padding: 3px 3px 3px 8px;
		color: var(--window-light);
		font-weight: 700;
		background: linear-gradient(90deg, var(--title-inactive-start), var(--title-inactive-end));
		user-select: none;
		cursor: grab;
		touch-action: none;
	}

	.window.is-dragging {
		z-index: 60;
		box-shadow: 5px 5px 0 #000, 10px 10px 30px rgba(0, 0, 0, 0.42);
		user-select: none;
	}

	.window.is-dragging .title-bar {
		cursor: grabbing;
	}

	.window.is-active .title-bar {
		background: linear-gradient(90deg, var(--title-start), var(--title-end));
	}

	.title-text {
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	.win-button {
		background: var(--window-bg);
		color: var(--ink);
		border-top: 2px solid var(--window-light);
		border-left: 2px solid var(--window-light);
		border-right: 2px solid var(--window-shadow);
		border-bottom: 2px solid var(--window-shadow);
		font-family: "MS Sans Serif", Tahoma, Geneva, sans-serif;
		font-size: 12px;
		line-height: 1;
		text-decoration: none;
		cursor: pointer;
	}

	.win-button:focus-visible {
		outline: 1px dotted var(--ink);
		outline-offset: -4px;
	}

	.win-button:active,
	.win-button.is-selected {
		border-top-color: var(--window-shadow);
		border-left-color: var(--window-shadow);
		border-right-color: var(--window-light);
		border-bottom-color: var(--window-light);
	}

	.window-body {
		margin: 3px;
		padding: 18px;
		background: var(--window-bg);
		border-top: 1px solid var(--window-mid);
		border-left: 1px solid var(--window-mid);
		border-right: 1px solid var(--window-light);
		border-bottom: 1px solid var(--window-light);
		line-height: 1.45;
		min-width: 0;
		overflow-wrap: break-word;
	}

	.profile-text {
		min-width: 0;
		overflow-wrap: break-word;
	}

	.profile-body {
		display: grid;
		grid-template-columns: 300px 1fr;
		gap: 22px;
		align-items: start;
	}

	.profile-photo-frame {
		padding: 6px;
		background: var(--window-panel);
		border-top: 2px solid var(--window-shadow);
		border-left: 2px solid var(--window-shadow);
		border-right: 2px solid var(--window-light);
		border-bottom: 2px solid var(--window-light);
	}

	.profile-photo {
		display: block;
		width: 100%;
		height: 300px;
		object-fit: cover;
		object-position: center;
	}

	.profile-text h1 {
		margin: 0 0 6px;
		font-size: 44px;
		line-height: 1;
		letter-spacing: -0.5px;
	}

	.profile-text .caption {
		margin-top: -2px;
		margin-bottom: 10px;
		font-family: "Courier New", ui-monospace, monospace;
		font-size: 12px;
	}

	.profile-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 8px;
		margin: 8px 0 0;
	}

	.profile-actions .win-button {
		border-top: 2px solid var(--window-light);
		border-left: 2px solid var(--window-light);
		border-right: 2px solid var(--window-shadow);
		border-bottom: 2px solid var(--window-shadow);
		padding: 6px 12px;
		font-weight: 700;
	}

	.profile-text p {
		margin: 0 0 12px;
	}

	.profile-text a {
		color: var(--ink);
		text-decoration: none;
		border-bottom: 1px dotted var(--ink-muted);
	}

	.profile-text a:hover,
	.profile-text a:focus-visible {
		color: var(--ink-link);
		border-bottom-color: var(--ink-link);
	}

	.profile-text .reach-out {
		margin-top: 4px;
		margin-bottom: 0;
		color: var(--ink-muted);
	}

	h2 {
		margin: 4px 0 6px;
		font-size: 15px;
		line-height: 1.3;
	}

	.publication h2 a {
		color: var(--ink);
		text-decoration: none;
		border-bottom: 1px dotted var(--ink-muted);
	}

	.publication h2 a:hover,
	.publication h2 a:focus-visible {
		color: var(--ink-link);
		border-bottom-color: var(--ink-link);
	}

	p {
		margin: 0 0 10px;
	}

	.caption,
	.venue {
		color: var(--ink-muted);
		font-weight: 700;
	}

	.venue {
		display: inline-block;
		margin-bottom: 4px;
		padding: 1px 6px;
		background: #dad5c8;
		border-top: 1px solid var(--window-shadow);
		border-left: 1px solid var(--window-shadow);
		border-right: 1px solid var(--window-light);
		border-bottom: 1px solid var(--window-light);
		font-size: 11px;
		letter-spacing: 0.4px;
		text-transform: uppercase;
	}

	.thread-buttons,
	.toolbar {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 7px;
		margin-bottom: 12px;
	}

	.toolbar-hint {
		margin-left: auto;
		color: var(--ink-muted);
		font-weight: 700;
		font-size: 11px;
	}

	.win-button {
		display: inline-flex;
		align-items: center;
		min-height: 25px;
		padding: 5px 10px;
	}

	.dos-panel {
		min-height: 72px;
		margin-bottom: 4px;
		padding: 14px 14px 12px;
		background: #000;
		color: var(--terminal-text);
		border-top: 2px solid var(--window-shadow);
		border-left: 2px solid var(--window-shadow);
		border-right: 2px solid var(--window-light);
		border-bottom: 2px solid var(--window-light);
		font-family: "Courier New", ui-monospace, monospace;
		font-size: 13px;
		line-height: 1.55;
		white-space: normal;
		overflow-wrap: anywhere;
	}

	.dos-panel .prompt {
		color: var(--terminal-accent);
	}

	.dos-panel .caret {
		display: inline-block;
		width: 9px;
		height: 15px;
		margin-left: 3px;
		vertical-align: text-bottom;
		background: var(--terminal-text);
		animation: caret-blink 1s steps(2, start) infinite;
	}

	@keyframes caret-blink {
		to { opacity: 0; }
	}

	@media (prefers-reduced-motion: reduce) {
		.dos-panel .caret { animation: none; }
	}

	.publication-scroll {
		--scroll-thumb-height: 72px;
		--scroll-thumb-top: 0px;
		position: relative;
		padding-right: 22px;
	}

	.publication-scroll.is-scroll-dragging {
		user-select: none;
	}

	.publication-list {
		display: grid;
		grid-template-columns: minmax(0, 1fr);
		gap: 10px;
		max-height: clamp(260px, 38svh, 340px);
		overflow-y: scroll;
		overscroll-behavior: contain;
		scrollbar-width: none;
	}

	.publication-list::-webkit-scrollbar {
		width: 0;
		height: 0;
	}

	.publication-scrollbar {
		position: absolute;
		top: 0;
		right: 0;
		bottom: 0;
		width: 14px;
		background: var(--window-bg);
		border-top: 1px solid var(--window-shadow);
		border-left: 1px solid var(--window-mid);
		border-right: 1px solid var(--window-light);
		border-bottom: 1px solid var(--window-light);
		cursor: pointer;
	}

	.publication-scrollbar::before {
		content: "";
		position: absolute;
		inset: 2px;
		background:
			repeating-linear-gradient(
				45deg,
				rgba(52, 50, 48, 0.14) 0,
				rgba(52, 50, 48, 0.14) 1px,
				transparent 1px,
				transparent 4px
			);
	}

	.publication-scrollbar-thumb {
		position: absolute;
		top: var(--scroll-thumb-top);
		left: 2px;
		right: 2px;
		height: var(--scroll-thumb-height);
		min-height: 44px;
		background: var(--window-mid);
		border-top: 2px solid var(--window-light);
		border-left: 2px solid var(--window-light);
		border-right: 2px solid var(--window-shadow);
		border-bottom: 2px solid var(--window-shadow);
		box-shadow: 0 0 0 1px rgba(17, 17, 17, 0.18);
		cursor: grab;
		touch-action: none;
	}

	.publication-scroll.is-scroll-dragging .publication-scrollbar-thumb {
		cursor: grabbing;
	}

	.publication {
		padding: 10px;
		background: var(--window-panel);
		border-top: 2px solid var(--window-shadow);
		border-left: 2px solid var(--window-shadow);
		border-right: 2px solid var(--window-light);
		border-bottom: 2px solid var(--window-light);
	}

	.publication .note {
		margin-bottom: 0;
	}

	.thoughts-list {
		list-style: none;
		padding: 0;
		margin: 0;
		display: grid;
		gap: 8px;
	}

	.thoughts-item { margin: 0; }

	.thoughts-link {
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 10px;
		align-items: baseline;
		padding: 8px 10px;
		text-decoration: none;
		color: var(--ink);
		background: var(--window-panel);
		border-top: 2px solid var(--window-shadow);
		border-left: 2px solid var(--window-shadow);
		border-right: 2px solid var(--window-light);
		border-bottom: 2px solid var(--window-light);
	}

	.thoughts-link:hover,
	.thoughts-link:focus-visible {
		background: #ffffff;
		outline: 1px dotted var(--ink);
		outline-offset: -4px;
	}

	.thoughts-date {
		font-family: "Courier New", ui-monospace, monospace;
		font-size: 11px;
		color: var(--ink-muted);
		font-weight: 700;
		letter-spacing: 0.3px;
		text-transform: uppercase;
		white-space: nowrap;
	}

	.thoughts-title {
		font-weight: 700;
		line-height: 1.3;
	}

	.link-list {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 4px 12px;
	}

	.link-list a,
	.link-list button {
		display: flex;
		align-items: center;
		gap: 10px;
		padding: 6px 6px;
		color: var(--ink);
		background: transparent;
		font: inherit;
		text-decoration: none;
		border: 1px solid transparent;
		cursor: pointer;
	}

	.link-list a:hover,
	.link-list a:focus-visible,
	.link-list button:hover,
	.link-list button:focus-visible {
		background: #c9c4b8;
		border-color: var(--window-shadow);
		outline: none;
	}

	.copy-toast {
		position: fixed;
		right: 24px;
		bottom: 24px;
		z-index: 80;
		display: flex;
		align-items: center;
		gap: 14px;
		width: min(360px, calc(100vw - 28px));
		padding: 12px 12px 12px 14px;
		color: #f9f6ef;
		background: rgba(17, 17, 17, 0.94);
		border: 1px solid rgba(255, 255, 255, 0.18);
		box-shadow: 0 18px 44px rgba(0, 0, 0, 0.38);
		opacity: 0;
		transform: translateY(10px);
		transition:
			opacity 160ms ease,
			transform 160ms ease;
	}

	.copy-toast[hidden] {
		display: none;
	}

	.copy-toast.is-visible {
		opacity: 1;
		transform: translateY(0);
	}

	.copy-toast.is-error {
		background: rgba(48, 22, 22, 0.96);
	}

	.copy-toast-text {
		display: grid;
		gap: 2px;
		min-width: 0;
	}

	.copy-toast-text strong {
		font-size: 13px;
		line-height: 1.2;
	}

	.copy-toast-input {
		width: 100%;
		min-width: 0;
		padding: 0;
		color: #d8d1c2;
		background: transparent;
		border: 0;
		font-family: "Courier New", ui-monospace, monospace;
		font-size: 12px;
		line-height: 1.3;
		text-overflow: ellipsis;
	}

	.copy-toast-input:focus {
		color: #f9f6ef;
		outline: 1px dotted rgba(255, 255, 255, 0.72);
		outline-offset: 2px;
	}

	.copy-toast-close {
		flex: 0 0 auto;
		margin-left: auto;
		padding: 5px 8px;
		color: #111111;
		background: #e6e2d8;
		border-top: 2px solid #ffffff;
		border-left: 2px solid #ffffff;
		border-right: 2px solid #343230;
		border-bottom: 2px solid #343230;
		font: inherit;
		font-size: 11px;
		font-weight: 700;
		cursor: pointer;
	}

	.copy-toast-close:focus-visible {
		outline: 1px dotted #ffffff;
		outline-offset: 2px;
	}

	.copy-toast-close:active {
		border-top-color: #343230;
		border-left-color: #343230;
		border-right-color: #ffffff;
		border-bottom-color: #ffffff;
	}

	.small-icon {
		display: inline-grid;
		place-items: center;
		width: 24px;
		height: 20px;
		background: var(--window-panel);
		color: var(--ink);
		border-top: 1px solid var(--window-light);
		border-left: 1px solid var(--window-light);
		border-right: 1px solid var(--window-shadow);
		border-bottom: 1px solid var(--window-shadow);
		font-size: 10px;
		font-weight: 700;
		letter-spacing: 0.4px;
	}

	@media (max-width: 1100px) {
		.desktop-layout {
			grid-template-columns: minmax(0, 1fr);
			grid-template-areas:
				"main"
				"side";
		}
		.profile-body {
			grid-template-columns: 240px 1fr;
		}
		.link-list {
			grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
		}
	}


	@media (max-width: 760px) {
		.win98-desktop {
			min-height: auto;
			padding: 16px 14px 28px;
		}

		.desktop-layout {
			grid-template-columns: 1fr;
			grid-template-areas:
				"main"
				"side";
			gap: 16px;
			max-width: none;
		}

		.stack { gap: 16px; }

		.profile-body {
			grid-template-columns: 1fr;
		}

		.profile-photo-frame {
			justify-self: start;
			max-width: 280px;
		}

		.profile-photo {
			width: 100%;
			height: auto;
			aspect-ratio: 1 / 1;
		}

		.profile-text h1 {
			font-size: 36px;
		}

		.stack-side .links-window {
			order: 1;
		}

		.stack-side .papers-window {
			order: 2;
		}

		.stack-side .thoughts-window {
			order: 3;
		}

		.copy-toast {
			right: 14px;
			bottom: 14px;
		}
	}

	@media (hover: none), (pointer: coarse), (max-width: 760px) {
		.window {
			transform: none !important;
		}

		.title-bar {
			cursor: default;
			touch-action: auto;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.copy-toast {
			transition: none;
		}
	}
</style>
